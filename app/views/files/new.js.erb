$('#mainModal').modal('show');
$('.modal-title').html("<%= t(:new_file) %>");
$('.form').html("<%= j (render 'files/new') %>");

date = new Date();

$('.datetimepicker').datetimepicker({
   minDate:date,
   minTime:date.getTime()
});