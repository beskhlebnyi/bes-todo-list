tr id="list-#{list.id}"
  td = link_to list.title, list_tasks_path(list), remote: true
  td = link_to 'Edit', edit_list_path(list), remote: true
  td = link_to 'Destroy', list, data: { confirm: 'Are you sure?' }, method: :delete, remote: true