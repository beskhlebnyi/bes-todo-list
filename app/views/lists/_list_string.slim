td = link_to " #{list.title}", list_tasks_path(list), remote: true, class:'fa fa-bars'
td = link_to ' Edit', edit_list_path(list), remote: true, class:'fa fa-edit'
td = link_to ' Delete', list, data: { confirm: 'Are you sure?' }, method: :delete, remote: true, class:'fa fa-trash'