$('#mainModal').modal('show');
$('.modal-title').html("<%= t(:new_document) %>");
$('.form').html("<%= j (render 'documents/new') %>");

date = new Date();

$('.datetimepicker').datetimepicker({
   minDate:date,
   minTime:date.getTime()
});