$('#mainModal').modal('show');
$('.modal-title').html("<%= t(:new_task) %>");
$('.form').html("<%= j (render 'tasks/new') %>");

date = new Date();

$('.datetimepicker').datetimepicker({
   minDate:date,
   minTime:date.getTime()
});