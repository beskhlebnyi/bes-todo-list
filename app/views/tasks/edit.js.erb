$('#mainModal').modal('show');
$('.modal-title').html("<%= t(:edit_task) %>");
$('.form').html("<%= j (render 'tasks/edit') %>");

date = new Date();

$('.datetimepicker').datetimepicker({
   minDate:date,
   minTime:date.getTime()
});