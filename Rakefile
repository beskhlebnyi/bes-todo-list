# Add your own files in files placed in lib/files ending in .rake,
# for example lib/files/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_files
